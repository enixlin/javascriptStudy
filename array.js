/**
 * 
 * 这里学习关于数组的一切，一切！！！ join() slice() splice() sort()
 * 
 */

var array1 = new Array("a", "b", "c");
var array2 = new Array("1", "2", "3");

var array3 = array1.join("-");// 将本数组内所有的元素用一个指定的字符连接起来，成来一个新的字符串
var array4 = array1.concat(array2);// 将两个数组连接起来
// array.slice(start, end),返回数组的一部份,不含end
var array5 = array1.slice(0, 2);
/**
 * this is a test class about xxx
 * @param {} name
 * @param {} age
 * @return {}
 */
function fds(name,age) {
	Ext.Ajax.request("", {});
	return fdsfs;
}
console.log(array1);
console.log(array2);
console.log(array3);
console.log(array4);
console.log(array5);
